# MovieHub — лабораторные работы по веб-программированию

Единый многостраничный портал о кино на HTML, CSS и TypeScript. Каждая новая лабораторная расширяет проект. Клиентская часть собирается через Vite, а полноценная авторизация работает на Node.js, Express и SQLite.

## Запуск

Требуется Node.js 22+. Для поиска фильмов создайте в корне локальный `.env` по образцу `.env.example` и укажите `OMDB_API_KEY`. Бесплатный ключ можно получить на [сайте OMDb](https://www.omdbapi.com/apikey.aspx) и активировать по ссылке из письма. После изменения `.env` перезапустите сервер. Не добавляйте ключ в переменные `VITE_*`, Git или экспортируемые окружения Postman.

```bash
npm install
npm run dev
```

Команда одновременно запускает Vite и API. Откройте адрес, который выведет Vite (обычно `http://localhost:5173`). База автоматически появится в `data/moviehub.db` и не попадёт в Git.

Для запуска собранной версии:

```bash
npm run build
npm start
```

Затем откройте `http://127.0.0.1:3000`.

## Проверка и сборка

```bash
npm run typecheck
npm test
npm run build
npm run check
```

## Страницы

- `index.html` — главная страница, слайд-шоу и видео.
- `movies.html` — поиск реальных фильмов OMDb, фильтр по году, страницы результатов и учебные жанровые блоки.
- `movie.html?id=tt0816692` — подробная карточка фильма из OMDb.
- `about.html` — информация о портале и учебные примеры.
- `auth.html` — формы входа и регистрации с клиентской и серверной валидацией.
- `profile.html` — защищённый личный кабинет и выход из аккаунта.

## Лабораторная №1

| Пункт | Реализация |
| --- | --- |
| 1.1 | На страницах используются заголовки `h1`–`h6` |
| 1.2 | Текст и блоки размечены через `p`, `span`, `div` |
| 1.3 | Используются `em` и `strong` |
| 1.4 | В приветственном тексте есть перенос `br` |
| 1.5 | На страницах есть списки `ol` и `ul` |
| 1.6 | Локальные изображения SVG и видео MP4 |
| 1.7 | Ссылки размечены тегами `a` |
| 1.8 | Есть якорная навигация по разделам страницы |
| 1.9 | Главное меню связывает три HTML-страницы |
| 2.1 | Внешний CSS, блок `style` в `movies.html`, inline-стиль в `about.html` |
| 2.2 | Карточки демонстрируют `background`, `border`, `margin`, `padding` |
| 2.3 | Применяются селекторы тегов и классов |
| 2.4 | Цвет текста карточек демонстрирует наследование и каскадирование |
| 2.5 | Локальный FilmSans, внешний Oswald и системные шрифты |
| 2.6 | CSS-анимации, hover-эффекты и переходы |
| 2.7 | Фиксированное выдвижное меню работает на TypeScript |
| 2.8 | На главной странице работает слайд-шоу |

## Лабораторная №2

| Требование | Реализация |
| --- | --- |
| Форма авторизации/регистрации | Две переключаемые формы на `auth.html` |
| `:focus` | Активное поле получает светлую рамку и фон |
| `:valid` / `:invalid` | После взаимодействия поля подсвечиваются зелёным или красным |
| `:required` / `:optional` | Обязательные и необязательные поля имеют разный левый край |
| HTML-валидация | `required`, `type`, `pattern`, `minlength`, `maxlength`, `min`, `max` |
| Проверка по маске | Телефон форматируется как `+7 (999) 999-99-99` |
| Минимум и максимум | Возраст 14–100, пароль 8–24, текстовые ограничения длины |
| Имя и фамилия | Только буквы, первая заглавная, остальные строчные |
| JavaScript/DOM | TypeScript обрабатывает события, ошибки, запросы и переход в профиль |
| Серверная валидация | Критичные правила повторяются в Express API |
| Регистрация и вход | Пользователь сохраняется в SQLite и может повторно войти |
| Пароли | В базе хранится bcrypt-хеш, открытый пароль не сохраняется |
| Сессия | Случайный токен передаётся в `HttpOnly`, `SameSite=Lax` cookie; в SQLite хранится только его SHA-256-хеш |
| Защита | Helmet добавляет HTTP-заголовки, попытки входа и регистрации ограничены по частоте |
| Профиль и выход | `/profile.html` загружает текущего пользователя через API и позволяет завершить сессию |

### API авторизации

| Метод и путь | Назначение |
| --- | --- |
| `POST /api/auth/register` | Создать аккаунт и сессию |
| `POST /api/auth/login` | Войти по email и паролю |
| `GET /api/auth/me` | Получить текущего пользователя |
| `POST /api/auth/logout` | Завершить текущую сессию |
| `GET /api/health` | Проверить работу сервера |

## Лабораторная №3

Выбран вариант работы с внешним кино-API OMDb вместо Telegram. Поиск доступен гостям и авторизованным пользователям; аккаунты и SQLite из второй лабораторной не изменены.

| Требование | Реализация |
| --- | --- |
| Внешний API | HTTPS-запросы Express → OMDb через native fetch |
| REST | `GET /api/movies?query=Interstellar&year=2014&page=1` и `GET /api/movies/tt0816692` |
| Интерфейс | Поиск, год, пагинация, постеры и подробности фильма |
| Обработка ошибок | Загрузка, пустые результаты, неверные параметры, отсутствующий фильм, проблемы ключа, квоты и сети |
| Безопасность | Ключ только в серверном `.env`; нормализация JSON, безопасный текст и разрешённые домены постеров |
| Квота | Поиск по кнопке, кэш на 10 минут (максимум 100 ответов), 30 запросов в минуту на IP, таймаут 8 секунд |
| Postman | Коллекция v2.1, пустое безопасное окружение, проверки ответов и инструкция демонстрации |

Форма обратной связи и Telegram не входят в согласованную замену. Если преподаватель требует их отдельно, нужна дополнительная реализация.

Файлы для импорта и инструкция: [postman/README.md](postman/README.md). Основная папка коллекции работает без ввода ключа в Postman. Поиск ориентирован на оригинальные названия, данные OMDb преимущественно на английском; платный Poster API не используется. Источник данных: [OMDb](https://www.omdbapi.com/), лицензия [CC BY-NC 4.0](https://creativecommons.org/licenses/by-nc/4.0/).

## Структура

```text
web-labs/
├── index.html
├── movies.html
├── movie.html
├── about.html
├── auth.html
├── profile.html
├── server/
│   ├── database.ts
│   ├── index.ts
│   ├── omdb.ts
│   └── validation.ts
├── src/
│   ├── auth.ts
│   ├── main.ts
│   ├── movies.ts
│   ├── movie.ts
│   ├── profile.ts
│   └── styles.css
├── tests/omdb.test.ts
├── postman/
└── public/assets/
    ├── fonts/
    ├── poster-*.svg
    └── moviehub-intro.mp4
```
